package spy;
public class SpyNetwork {
  /*  public static void main(String[] args) {
        WeightedGraph<Spy> spyNetwork = new WeightedGraph<>();
        
        Spy spy1 = new Spy("Dante");
        Spy spy2 = new Spy("Ema");
        Spy spy3 = new Spy("Nacho");

        int spy1Id = spyNetwork.put(spy1);
        int spy2Id = spyNetwork.put(spy2);
        int spy3Id = spyNetwork.put(spy3);
        
        spyNetwork.setConnection(spy1Id, spy2Id, 0.5); 
        spyNetwork.setConnection(spy2Id, spy3Id, 0.3); 
        spyNetwork.setConnection(spy1Id, spy3Id, 0.2);
        spyNetwork.setConnection(spy3Id, spy1Id, 0.1);
        spyNetwork.setConnection(spy2Id, spy1Id, 0.4);
        spyNetwork.setConnection(spy3Id, spy2Id, 0.6);
    }*/
}
